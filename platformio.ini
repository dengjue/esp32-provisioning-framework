; PlatformIO project file — optional; Arduino IDE works without this file.
; Default board targets ESP32-S3 (GPIO 48 NeoPixel). Change board if needed.

[platformio]
src_dir = .

[env]
extra_scripts = pre:scripts/load_env.py
monitor_speed = 115200
build_src_filter =
    +<esp32-provisioning-framework.ino>
    +<esp32-provisioning-framework.ino.cpp>
    +<src/>
    -<.workbuddy/>
lib_deps =
    h2zero/NimBLE-Arduino@^2.1.0
    knolleary/PubSubClient@^2.8
    adafruit/Adafruit NeoPixel@^1.12.0
    bblanchon/ArduinoJson@^7.0.0

[env:esp32-s3-devkitc-1]
extends = env
platform = espressif32
board = esp32-s3-devkitc-1
framework = arduino
build_flags =
    -Isrc
    -DARDUINO_USB_CDC_ON_BOOT=1

[env:esp32dev]
extends = env
platform = espressif32
board = esp32dev
framework = arduino
build_flags =
    -Isrc
    -DLED_PIN=2
